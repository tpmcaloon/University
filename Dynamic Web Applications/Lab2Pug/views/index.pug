doctype html
html
  head
    title= "Welcome to " + shopData.shopName
  body
    h1= shopData.shopName
    h2 Here are some things that we sell:
    ul
    each val in shopData.length ? values : ['There are no values']
      li= shopData.productCategories